{% extends 'base.html.twig' %}

{% block title %}Product Creation{% endblock %}

{% block body %}

        {{ form_start(productForm) }}
        {{ form_row(productForm.name) }}
        {{ form_row(productForm.price) }}

        <button type="submit" class="m-4 text-white bg-green-700 hover:bg-green-800 focus:outline-none focus:ring-4 focus:ring-green-300 font-medium rounded-full text-sm px-5 py-2.5 text-center me-2 mb-2 dark:bg-green-600 dark:hover:bg-green-700 dark:focus:ring-green-800">
                Créer nouveau produit
        </button>
        {{ form_end(productForm) }}

{% endblock %}
